provider "aws" {
  # NOTE: inherited from environment variable 'AWS_DEFAULT_REGION' or
  #       define with by setting
  # region = "us-east-1"

  # NOTE: set named profile that you defined yourself in ~/.aws
  # profile = "edu"
}
