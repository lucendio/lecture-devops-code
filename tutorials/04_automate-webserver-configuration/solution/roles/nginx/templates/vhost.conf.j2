server {

    listen 80;
    listen [::]:80;

    server_name {{ fqdn | default('""') }};

    root {{ web_root_path }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /health {
        return 200;
    }
}
